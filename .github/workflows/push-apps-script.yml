name: Push Apps Script

on:
  push:
    paths:
      - 'scripts/SessionNoteProcessor.gs'

jobs:
  push-to-apps-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install clasp
        run: npm install -g @google/clasp

      - name: Write clasp credentials
        run: echo '${{ secrets.CLASP_CREDENTIALS }}' > ~/.clasprc.json

      - name: Write .clasp.json
        run: echo '{"scriptId":"${{ secrets.SCRIPT_ID }}","rootDir":"./scripts"}' > .clasp.json

      - name: Rename to Code.gs for clasp
        run: cp scripts/SessionNoteProcessor.gs scripts/Code.gs

      - name: Push to Apps Script
        run: clasp push --force
